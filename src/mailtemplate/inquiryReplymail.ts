import { IInquiry } from "../dto/inquiry/inquiry.interface";

export function inquiryReplyMailTemplate(data: IInquiry): string {
  return `
  <!DOCTYPE html>
  <html lang="en">
    <head>
      <meta charset="UTF-8" />
      <meta name="viewport" content="width=device-width, initial-scale=1.0" />
      <title>New Inquiry - Plaza Sales</title>
      <style>
        * { box-sizing: border-box; }
        body { margin: 0; padding: 0; font-family: Arial, sans-serif; background-color: #f8f9fa; color: #333; }
        .container { max-width: 600px; margin: 20px auto; background: #fff; border-radius: 10px; box-shadow: 0 4px 10px rgba(0,0,0,0.1); overflow: hidden; }
        .header { background: #DA2B34; color: #fff; text-align: center; padding: 20px; }
        .header img { width: 100%; max-width: 80px; height: auto; border-radius: 50%; background: #fff; padding: 8px; margin-bottom: 10px; }
        .content { padding: 20px; max-height: 400px; overflow-y: auto; }
        .content h2 { color: #004aad; margin-top: 0; }
        .content p { font-size: 15px; line-height: 1.6; margin: 6px 0; }
        .divider { border-top: 1px solid #ddd; margin: 15px 0; }
        .footer { background: #f1f1f1; text-align: center; padding: 15px; font-size: 13px; color: #666; }
        @media screen and (max-width: 600px) { .container { margin: 10px; } .content { padding: 15px; } }
        @media screen and (max-width: 480px) { .header img { max-width: 60px; padding: 6px; } }
      </style>
    </head>
    <body>
      <div class="container">
        <div class="header">
          <img src="https://pub-969c218ddb2f4a29bbe25940c8760402.r2.dev/uploads/main-logo.png" alt="Plaza Sales Logo" />
          <h1>Plaza Sales</h1>
        </div>
        <div class="content">
          <h2>📩 New Inquiry Reply</h2>
          <p><strong>Name:</strong> Hello, ${data.name}</p>
          ${
            data.address
              ? `<p><strong>Address:</strong> ${data.address}</p>`
              : ""
          }
          <div class="divider"></div>
          <p><strong>Product:</strong> ${data.productId}</p>
          ${
            data.message
              ? `<div class="divider"></div><p><strong>Message:</strong> ${data.message}</p>`
              : ""
          }
        </div>
        <div class="footer">
          <p>This message was automatically generated by <strong>Plaza Sales</strong>.</p>
          <p>© ${new Date().getFullYear()} Plaza Sales. All rights reserved.</p>
        </div>
      </div>
    </body>
  </html>
  `;
}
